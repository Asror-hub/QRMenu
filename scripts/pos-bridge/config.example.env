# Copy to .env and edit
# Optional: if not set, orders are only logged (no printing)

# Printer IP address (WiFi/network thermal printer on same network)
PRINTER_IP=192.168.1.100

# Printer port (default 9100 for most thermal printers)
PRINTER_PORT=9100

# Bridge port (default 3000)
PORT=3000
